# ============================================================================
# Shell App Environment Variables - Example Template
# ============================================================================
# Copy this file to .env (development) and update values as needed.
# For production, create .env.production with similar structure.

# Port for webpack dev server
PORT=3000

# ============================================================================
# Feature Module Flags
# ============================================================================
# Control which feature modules are available in the UI
# Set to 'false' to disable a module (compile-time exclusion)
# Default: true (both enabled)

ENABLE_K11_INBOX=true
ENABLE_K11_MONITORING=true

# ============================================================================
# Module Federation Remote URLs (OPTIONAL - Build-Time Remotes)
# ============================================================================
# NOTE: For Docker/CDN architecture with runtime remotes, these are NOT needed!
# URLs come from your Java backend API (/api/plugins) at runtime.
#
# Only set these if you want to use build-time remotes (static configuration):
# - For development with known remote URLs (Option 2: Separate Dev Servers)
# - For static deployments with fixed module locations
#
# Leave empty or unset to use:
# 1. Runtime remotes from backend API (recommended for Docker/CDN architecture)
# 2. Local bundling (automatically uses src/ in dev, dist/ in prod)

# REMOTE_INBOX_URL=
# REMOTE_MONITORING_URL=

# Examples (only if using build-time remotes):
# REMOTE_INBOX_URL=http://localhost:3001/remoteEntry.js
# REMOTE_MONITORING_URL=http://localhost:3002/remoteEntry.js
# REMOTE_INBOX_URL=https://cdn.example.com/inbox/remoteEntry.js
# REMOTE_MONITORING_URL=https://cdn.example.com/monitoring/remoteEntry.js
